{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    // Node types only so tests can use node:test; the build config removes them.
    "types": ["node"],
    "noEmit": true
  },
  "include": [".", "../shared"]
}
